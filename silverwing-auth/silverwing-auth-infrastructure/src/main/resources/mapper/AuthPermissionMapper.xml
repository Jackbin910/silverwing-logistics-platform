<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.silverwing.auth.iam.infrastructure.dao.AuthPermissionDao">

    <!-- 根据用户ID查询权限标识列表（用户 -> 角色 -> 权限） -->
    <select id="selectPermissionCodesByUserId" resultType="java.lang.String">
        SELECT p.permission_code
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND p.deleted = 0
          AND p.status = 1
        GROUP BY p.permission_code, p.sort
        ORDER BY p.sort
    </select>

</mapper>
