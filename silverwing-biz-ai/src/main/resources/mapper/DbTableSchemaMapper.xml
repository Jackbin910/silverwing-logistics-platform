<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.silverwing.biz.ai.infrastructure.mapper.DbTableSchemaMapper">

    <resultMap id="DbTableSchemaResultMap" type="com.silverwing.biz.ai.infrastructure.dao.po.DbTableSchemaPO">
        <id property="id" column="id"/>
        <result property="databaseName" column="database_name"/>
        <result property="tableName" column="table_name"/>
        <result property="tableComment" column="table_comment"/>
        <result property="columnName" column="column_name"/>
        <result property="columnComment" column="column_comment"/>
        <result property="dataType" column="data_type"/>
        <result property="isPrimaryKey" column="is_primary_key"/>
        <result property="isNullable" column="is_nullable"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, database_name, table_name, table_comment, column_name, column_comment,
        data_type, is_primary_key, is_nullable, create_time, update_time
    </sql>

    <!-- 获取指定数据库的所有表（表名、表注释） -->
    <select id="getTables" resultMap="DbTableSchemaResultMap">
        SELECT table_name, table_comment
        FROM information_schema.tables
        WHERE table_schema = #{databaseName}
    </select>

    <!-- 获取指定表的列信息 -->
    <select id="getTableColumns" resultMap="DbTableSchemaResultMap">
        SELECT
            c.column_name,
            c.data_type,
            c.column_type,
            c.column_key,
            c.is_nullable,
            c.column_comment
        FROM information_schema.columns c
        WHERE c.table_schema = #{databaseName}
          AND c.table_name = #{tableName}
        ORDER BY c.ordinal_position
    </select>

</mapper>
