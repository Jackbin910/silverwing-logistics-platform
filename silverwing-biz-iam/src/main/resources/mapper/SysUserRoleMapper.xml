<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.silverwing.biz.iam.infrastructure.mapper.SysUserRoleMapper">

    <!-- 角色结果映射，JOIN 查询需显式限定各列，避免 id/deleted 等列歧义 -->
    <resultMap id="SysRoleResultMap" type="com.silverwing.biz.iam.domain.model.SysRole">
        <id property="id" column="id"/>
        <result property="roleCode" column="role_code"/>
        <result property="roleName" column="role_name"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectRolesByUserId" resultMap="SysRoleResultMap">
        SELECT r.id,
               r.role_code,
               r.role_name,
               r.status,
               r.create_by,
               r.update_by,
               r.create_time,
               r.update_time,
               r.deleted
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
          AND r.status = 1
    </select>

    <!-- 根据角色ID查询关联的用户ID列表 -->
    <select id="selectUserIdsByRoleId" resultType="java.lang.Long">
        SELECT user_id
        FROM sys_user_role
        WHERE role_id = #{roleId}
    </select>

</mapper>
